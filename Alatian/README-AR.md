# 🛒 Alatian - متجر قطع الكمبيوتر

متجر إلكتروني سعودي متخصص في تجميعات الكمبيوتر وقطع الألعاب بتصميم مشابه لـ TechTroniX.

## 📋 المتطلبات

قبل البدء، تأكد من توفر التالي:

- Node.js 18.0 أو أحدث
- Docker و Docker Compose (لقاعدة البيانات)
- pnpm أو npm أو yarn

## 🚀 التشغيل السريع

### 1. تشغيل قاعدة البيانات PostgreSQL

```bash
# من المجلد الرئيسي Alatian
docker-compose up -d
```

هذا سيشغل PostgreSQL على المنفذ 5432 بالإعدادات التالية:
- اسم المستخدم: `alatian`
- كلمة المرور: `alatian123`
- اسم القاعدة: `alatian`

### 2. إعداد Backend

```bash
cd backend

# نسخ ملف البيئة
cp .env.example .env

# تعديل ملف .env (إذا لزم الأمر)
# DATABASE_URL="postgresql://alatian:alatian123@localhost:5432/alatian?schema=public"

# تثبيت المكتبات
npm install

# تشغيل migrations
npx prisma migrate dev

# (اختياري) ملء قاعدة البيانات ببيانات تجريبية
npx prisma db seed

# تشغيل Backend
npm run start:dev
```

Backend سيعمل على: `http://localhost:3000`

### 3. إعداد Frontend

```bash
cd front-end

# تثبيت المكتبات
pnpm install

# تشغيل Frontend
pnpm run dev
```

Frontend سيعمل على: `http://localhost:3001`

## 🔧 حل المشاكل الشائعة

### خطأ: `Can't reach database server at localhost:5432`

**الحل:**
```bash
# تأكد من تشغيل Docker Compose
docker-compose up -d

# تحقق من أن PostgreSQL يعمل
docker-compose ps

# إذا لم يعمل، أعد تشغيله
docker-compose down
docker-compose up -d
```

### خطأ: `Cannot apply unknown utility class font-almarai`

**الحل:** تم الإصلاح! الخطوط الآن معرّفة بشكل صحيح في Tailwind CSS 4.x

### خطأ: Port already in use

**الحل:**
```bash
# للتحقق من المنفذ 3000 (Backend)
lsof -ti:3000 | xargs kill -9

# للتحقق من المنفذ 3001 (Frontend)
lsof -ti:3001 | xargs kill -9
```

## 📁 البنية

```
Alatian/
├── backend/                 # NestJS Backend
│   ├── src/
│   │   ├── auth/           # نظام المصادقة
│   │   ├── products/       # إدارة المنتجات
│   │   ├── cart/           # سلة التسوق
│   │   ├── orders/         # الطلبات
│   │   ├── reviews/        # التقييمات
│   │   ├── wishlist/       # المفضلة
│   │   └── build-pc/       # بناء الكمبيوتر
│   ├── prisma/
│   │   └── schema.prisma   # مخطط قاعدة البيانات
│   └── .env                # متغيرات البيئة
│
├── front-end/              # Next.js Frontend
│   ├── app/
│   │   ├── page.tsx       # الصفحة الرئيسية
│   │   ├── products/      # صفحة المنتجات
│   │   ├── cart/          # سلة التسوق
│   │   └── layout.tsx     # التخطيط الرئيسي
│   ├── components/
│   │   ├── navbar.tsx     # شريط التنقل
│   │   └── footer.tsx     # التذييل
│   ├── lib/
│   │   └── api.ts         # عميل API
│   └── .env.local         # متغيرات البيئة
│
└── docker-compose.yml      # إعداد PostgreSQL

## 🎨 المميزات

### تم الإنجاز ✅

- [x] تصميم حديث بألوان TechTroniX المميزة (#e55a1f)
- [x] وضع داكن افتراضي مع تدرجات لونية
- [x] دعم كامل للغة العربية مع RTL
- [x] تصميم متجاوب (Mobile, Tablet, Desktop)
- [x] Navbar مع dropdown للفئات
- [x] Footer متكامل
- [x] الصفحة الرئيسية مع Hero section
- [x] صفحة المنتجات مع فلاتر وبحث
- [x] Pagination متقدم
- [x] Loading states و Skeleton screens
- [x] تأثيرات بصرية (hover, glow, animations)
- [x] ربط كامل مع Backend API

### قيد التطوير 🚧

- [ ] صفحة تفاصيل المنتج
- [ ] صفحة سلة التسوق
- [ ] صفحة الدفع
- [ ] صفحة حسابي
- [ ] نظام المصادقة (Login/Register)
- [ ] لوحة تحكم الأدمن

## 🌐 API Endpoints

### Products
- `GET /products` - جلب المنتجات (مع فلاتر)
- `GET /products/:id` - تفاصيل منتج

### Cart
- `GET /cart` - جلب السلة
- `POST /cart` - إضافة منتج للسلة
- `PATCH /cart/:id` - تحديث كمية
- `DELETE /cart/:id` - حذف من السلة

### Auth
- `POST /auth/register` - التسجيل
- `POST /auth/login` - تسجيل الدخول
- `GET /auth/me` - معلومات المستخدم
- `POST /auth/logout` - تسجيل الخروج

### Orders
- `GET /orders` - جلب الطلبات
- `POST /orders` - إنشاء طلب
- `GET /orders/:id` - تفاصيل طلب

## 🎯 الخطوات التالية

1. **إضافة بيانات تجريبية**: أنشئ seed script لملء قاعدة البيانات
2. **صفحة تفاصيل المنتج**: عرض كامل للمنتج مع الصور والمواصفات
3. **سلة التسوق**: إدارة المنتجات في السلة
4. **نظام الدفع**: ربط مع بوابة دفع (مثل Moyasar أو Tap)
5. **لوحة الأدمن**: إدارة المنتجات والطلبات

## 🔐 الأمان

- JWT Authentication مع Refresh Tokens
- Password Hashing باستخدام bcrypt
- CORS محمي
- Input Validation باستخدام class-validator
- SQL Injection Protection عبر Prisma

## 📝 ملاحظات

- الخطوط العربية المستخدمة: Almarai & Tajawal
- الألوان الرئيسية:
  - Primary: `#e55a1f` (برتقالي TechTroniX)
  - Secondary: `#1a1a1a` (رمادي غامق)
  - Accent: `#ffa500` (برتقالي فاتح)

## 🤝 المساهمة

للمساهمة في المشروع:

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'إضافة ميزة رائعة'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. افتح Pull Request

## 📞 التواصل

للاستفسارات: info@alatian.com

## 📄 الترخيص

هذا المشروع خاص.

---

صُنع بـ ❤️ في السعودية
